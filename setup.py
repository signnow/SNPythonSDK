"""
Minimal setup.py for backward compatibility.
All package metadata and build configuration lives in pyproject.toml (PEP 517/518).
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
